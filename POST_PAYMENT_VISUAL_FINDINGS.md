# Post-Payment and Survey Visual Verification

- Desktop preview of `/bold-out-intake?token=...` shows the private survey heading, supporting copy, progress labels, question title, and controls in readable white/gold text on the dark branded background.
- Desktop preview of `/programs?bold-out=success&session_id=...` shows the successful payment thank-you panel with visible `Download workbook` and `Open private survey` actions.
- The thank-you copy clearly states that the personalized survey link and workbook are also sent by email and instructs registrants to check Spam, Junk, or Promotions if needed.
- The displayed test session produced a valid tokenized survey link in the preview query and did not expose a bare private-survey URL.

- Mobile preview at 375px shows the survey question title, prompt, progress labels, and answer controls visibly readable in white/gold contrast.
- Mobile preview shows the thank-you actions stacked cleanly: Add to calendar, Download workbook, and Open private survey; the Spam/Junk/Promotions guidance remains visible below.
